name: CI

on:
  push:
    branches: [ main ]    # or master if your default branch is called master
  workflow_dispatch:      # allows manual run from GitHub UI

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Run a simple test
        run: echo "Hello from GitHub Actions!"
